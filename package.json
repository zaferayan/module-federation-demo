{
  "name": "module-federation-demo",
  "description": "Minimal Module Federation demo with Vite + React — shell (host) consuming a remote ProductList component",
  "private": true,
  "scripts": {
    "dev": "concurrently -n shell,products -c blue,green \"npm run dev --prefix shell\" \"npm run dev --prefix products\"",
    "install:all": "npm install --prefix shell && npm install --prefix products",
    "setup": "npm install && npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
